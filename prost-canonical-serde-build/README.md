# prost-canonical-serde-build

Build helpers for `prost-canonical-serde` that configure `prost-build` with
canonical JSON settings for generated Rust types.

See `prost-canonical-serde` for usage and examples.
